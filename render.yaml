services:
  # Backend API Service
  - type: web
    name: movie-recommender-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: false
